. ~/.bash.d/aliases.sh
. ~/.bash.d/autojump.sh
. ~/.bash.d/vars.sh
. ~/.bash.d/rvm.sh
. ~/.bash.d/tmuxinator.sh
. ~/.bash.d/git.sh
. ~/.bash.d/completion.sh
. ~/.bash.d/git_completion.sh
. ~/.bash.d/private.sh

# keep this last so any functions defined above can be loaded
. ~/.bash.d/prompt.sh

set -o vi

# Load RVM into a shell session *as a function*
[[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" 
PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
